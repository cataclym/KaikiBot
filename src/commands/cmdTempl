import { Command } from "discord-akairo";
import { Message } from "discord.js";

export default class wdwdwdw extends Command {
	constructor() {
		super("tt", {
			aliases: ["tt"],
			description: { description: "",
				usage: "" },
		});
	}

	public async exec(): Promise<void> {
		return;
	}
}
